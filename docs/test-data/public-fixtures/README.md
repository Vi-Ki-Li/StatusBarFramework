# public-fixtures

可发布的结构化测试夹具目录。

## 规则

1. 仅允许 synthetic/sanitized 数据。
2. 不提交本地工作区数据与不可共享内容。
3. 每个夹具必须包含 `manifest.json` 与 `traceability.json`。

## 模板

请复制 `docs/test-data/public-fixtures/template/` 作为新夹具起点。
